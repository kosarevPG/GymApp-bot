# 🚀 Инструкция по деплою

## Деплой на Render.com (Backend) + GitHub Pages (Frontend)

### Шаг 1: Подготовка Google Credentials для Render.com

1. Откройте ваш `credentials.json` файл
2. Скопируйте **весь** JSON (от `{` до `}`)
3. Этот JSON нужно будет вставить в переменную окружения на Render.com

### Шаг 2: Деплой Frontend на GitHub Pages

1. Создайте новый репозиторий на GitHub (например, `gym-logger-frontend`)
2. Загрузите файл `index.html` в корень репозитория
3. Перейдите в Settings → Pages
4. В разделе "Source" выберите:
   - Branch: `main` (или `master`)
   - Folder: `/ (root)`
5. Нажмите Save
6. Через 2-3 минуты получите URL вида: `https://ваш-ник.github.io/gym-logger-frontend/`
7. **Важно**: Убедитесь, что URL заканчивается на `/` (слеш)

### Шаг 3: Деплой Backend на Render.com

#### 3.1. Подготовка репозитория

1. Создайте репозиторий на GitHub (например, `gym-logger-bot`)
2. Загрузите все файлы проекта:
   - `bot.py`
   - `google_sheets.py`
   - `requirements.txt`
   - `render.yaml` (опционально, для автоматической настройки)
   - `.gitignore`
   - `README.md`
3. **НЕ загружайте** `credentials.json` и `.env` (они в `.gitignore`)

#### 3.2. Создание Web Service на Render.com

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Заполните настройки:
   - **Name**: `gym-logger-bot` (или любое другое)
   - **Environment**: `Python 3`
   - **Region**: Выберите ближайший
   - **Branch**: `main` (или `master`)
   - **Root Directory**: оставьте пустым
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`
   - **Plan**: **Free** (для начала)

#### 3.3. Настройка переменных окружения

В разделе **Environment Variables** добавьте:

| Key | Value | Описание |
|-----|-------|----------|
| `BOT_TOKEN` | `ваш_токен_от_BotFather` | Токен Telegram бота |
| `WEBAPP_URL` | `https://ваш-ник.github.io/gym-logger-frontend/` | URL вашего фронтенда |
| `SPREADSHEET_ID` | `ID_вашей_таблицы` | ID Google Spreadsheet |
| `GOOGLE_CREDENTIALS_JSON` | `{весь JSON из credentials.json}` | **Весь JSON одной строкой** |
| `USE_WEBHOOK` | `false` | Использовать polling (для бесплатного тарифа) |
| `PORT` | Оставляем пустым | Render автоматически установит |

**Важно для GOOGLE_CREDENTIALS_JSON:**
- Скопируйте весь JSON из `credentials.json`
- Вставьте его как одну строку (без переносов)
- Пример: `{"type":"service_account","project_id":"...","private_key_id":"...",...}`

#### 3.4. Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и следуйте инструкциям
3. Получите токен и вставьте в `BOT_TOKEN` на Render
4. Отправьте `/newapp`:
   - Выберите вашего бота
   - Название: `Gym Logger`
   - Описание: `Трекер тренировок`
   - Фото: опционально
   - **Web App URL**: `https://ваш-ник.github.io/gym-logger-frontend/`
   - Short name: `gym-logger` (или другое)

#### 3.5. Запуск

1. Нажмите **Create Web Service** на Render
2. Дождитесь завершения деплоя (2-5 минут)
3. Проверьте логи - должны быть сообщения "Запуск бота..."
4. Откройте Telegram и отправьте `/start` вашему боту

### Шаг 4: Проверка работы

1. Отправьте `/start` боту в Telegram
2. Если бот не отвечает сразу - подождите 30-50 секунд (сервер "просыпается")
3. После первого ответа все должно работать быстро

## 🔧 Режимы работы

### Polling (рекомендуется для Free тарифа)

- `USE_WEBHOOK=false`
- Бот сам запрашивает обновления у Telegram
- Проще настроить
- На бесплатном тарифе Render сервер "засыпает" через 15 минут бездействия
- Первый запрос после "сна" может занять 30-50 секунд

### Webhook (для платных тарифов)

- `USE_WEBHOOK=true`
- `WEBHOOK_URL=https://your-bot.onrender.com/webhook`
- Telegram отправляет обновления на ваш сервер
- Требует постоянного доступного URL
- Работает быстрее, но сложнее настроить

## ⚠️ Важные замечания

1. **Бесплатный тариф Render.com:**
   - Сервер "засыпает" через 15 минут бездействия
   - Первый запрос после "сна" занимает 30-50 секунд
   - Для личного использования это нормально

2. **Google Credentials:**
   - Никогда не публикуйте `credentials.json` в открытом репозитории
   - Используйте переменную окружения `GOOGLE_CREDENTIALS_JSON` на Render

3. **HTTPS обязателен:**
   - Telegram требует HTTPS для WebApp
   - GitHub Pages и Render.com предоставляют HTTPS автоматически

4. **Проверка логов:**
   - На Render.com перейдите в раздел **Logs**
   - Там видны все сообщения бота и ошибки

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте логи на Render.com
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `BOT_TOKEN` правильный
4. Подождите 30-50 секунд (сервер может "просыпаться")

### Ошибка подключения к Google Sheets

1. Проверьте `GOOGLE_CREDENTIALS_JSON` - должен быть валидный JSON
2. Убедитесь, что Service Account имеет доступ к таблице
3. Проверьте `SPREADSHEET_ID` - правильный ли ID

### WebApp не открывается

1. Проверьте `WEBAPP_URL` - должен заканчиваться на `/`
2. Убедитесь, что `index.html` доступен по этому URL
3. Проверьте настройки Mini App в @BotFather

## 📝 Обновление кода

После изменений в коде:

1. Закоммитьте и запушьте в GitHub
2. Render.com автоматически перезапустит сервис
3. Дождитесь завершения деплоя (2-3 минуты)

